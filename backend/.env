
MONGODB_URI=mongodb://localhost:27017/healthhub
JWT_SECRET=healthhub_secret_key_2024_secure_random_string
PORT=5000

# Vertex AI (Gemini) settings
GCP_PROJECT_ID=
VERTEX_LOCATION=us-central1
VERTEX_MODEL=chat-bison@001
# Either set GOOGLE_APPLICATION_CREDENTIALS to a service account json path OR
# set GCP_SERVICE_ACCOUNT_JSON with the JSON content (not recommended for prod)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# GCP_SERVICE_ACCOUNT_JSON="{...}"